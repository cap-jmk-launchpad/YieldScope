apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: yieldscope-mail-data
  namespace: yieldscope-mail
  labels:
    app: yieldscope-mail
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
